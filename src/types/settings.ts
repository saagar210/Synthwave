export interface AppSettings {
  lastMode: string;
  lastThemeIndex: number;
  lastDeviceName: string | null;
  sensitivity: number;
  fftSize: number;
  targetFps: number;
  hasSeenWelcome: boolean;
}
